Session
    - id (surrogate key, sequential)
	- secret (randomized secret, likely unique)
    - created_on
	- master_addr
    - slave_addr
    - tx_partial
    - tx_partial_added_on
    - tx_id
    - submitted_on
